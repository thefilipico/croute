# c route

nesto

# asfds
## Upute za pokretanje

Backend kod se nalazi u `backend` mapi. Nakon kloniranja repozitorija potrebno je ući u mapu (`cd backend`) i instalirati sve s `npm install` pa pokrenuti server naredbom `npm run dev`
placeholder 